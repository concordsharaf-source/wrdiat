# الانتقال إلى مشروع Firebase جديد (wrdiat) — خطوة بخطوة

التطبيق الآن مربوط بالمشروع القديم `ytcalender-bae88` (من التطبيق السابق)، وكل البيانات فيه تحت
`sessions/_wrdiat_org/`. للانتقال إلى مشروعك الجديد **wrdiat** اتبع الخطوات بالترتيب.

---

## 1) تهيئة المشروع الجديد في الكونسول

**أ. إنشاء قاعدة بيانات Realtime Database (مهم — المشروع الجديد لا يملك قاعدة تلقائياً):**
1. افتح <https://console.firebase.google.com> ← اختر مشروع **wrdiat**.
2. القائمة الجانبية: **Build ← Realtime Database** ← **Create Database**.
3. اختر الموقع الأقرب (الأفضل نفس القديم: **asia-southeast1 / Singapore**).
4. اختر **Start in locked mode** (مقفلة) — لا تختر Test mode لأن صلاحيتها تنتهي بعد 30 يوماً وتتوقف الكتابة.
5. بعد الإنشاء: تبويب **Rules** ← الصق ثم **Publish**:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

> **بخصوص رسالة فايربيس:** «Your security rules are defined as public…» — هذه **تنبيه طبيعي** يظهر لأي قواعد عامة، ومعناه أن أي شخص يعرف رابط القاعدة يستطيع القراءة والكتابة مباشرة. وليست خطأ يمنع العمل.

### خيارات الأمان (اختر واحداً)

**الخيار 1 — عامة كما هي (الأبسط):** مناسبة لتطبيق داخلي صغير. التطبيق يحمي الواجهة بتسجيل دخول الأدمن، لكن من يعرف رابط القاعدة يستطيع الكتابة مباشرة.

**الخيار 2 — قواعد أضيق على مسار التطبيق فقط (بدون أي تعديل كود):** يقلّل المخاطرة لبقية محتوى القاعدة:

```json
{
  "rules": {
    "sessions": {
      "_wrdiat_org": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```

**الخيار 3 — تشديد كامل بتسجيل دخول Firebase (الأقوى، يحتاج تعديل كود في التطبيق):**
يُسجَّل الأدمن في Firebase Auth عند دخوله، وتصبح الكتابة محصورة بالمُسجَّلين:
`.read: true` للجميع (الاطلاع) + `.write: "auth != null"`.
بها تختفي إمكانية التعديل من خارج التطبيق — أخبرني لو أردتها وأضيفها.

---

### ⚠️ خطوة حرجة: هل أنشأت قاعدة البيانات بالوضع الصحيح؟

**لا تختر «Start in test mode»** عند الإنشاء — صلاحيته تنتهي بعد 30 يوماً وتتوقف الكتابة (وهذا هو سبب رسالة «تعذر الحفظ على الخادم» في المشروع القديم).
اختر **Start in locked mode** ثم الصق القواعد أعلاه في تبويب **Rules** واضغط **Publish**.
6. انسخ رابط القاعدة الظاهر أعلى تبويب **Data** — سيكون بهذا الشكل:
   `https://wrdiat-XXXXX-default-rtdb.asia-southeast1.firebasedatabase.app`

**ب. تسجيل تطبيق ويب لِجلب الإعدادات:**
1. ⚙️ **Project settings** ← تبويب **General** ← انزل إلى **Your apps**.
2. اضغط أيقونة **`</>`** (Web) ← اكتب الاسم `wrdiat-web` ← **Register app** (لا تحتاج Hosting).
3. سيظهر كائن **firebaseConfig** — انسخ القيم كلها (apiKey / authDomain / **databaseURL** / projectId / storageBucket / messagingSenderId / appId).

---

## 2) نقل البيانات القديمة (إن أردت نفس الأقسام والموظفين والورديات)

1. **من المشروع القديم** `ytcalender-bae88`: Realtime Database ← تبويب **Data**.
2. من قائمة **⋮** (أعلى يمين الجدول) اختر **Export JSON** — ينزل ملف على جهازك.
3. **في المشروع الجديد wrdiat**: Realtime Database ← **Data** ← **⋮** ← **Import JSON**.
4. اختر الملف ← Import (⚠️ الاستيراد يستبدل كامل قاعدة البيانات الجديدة).
5. ستجد نفس البنية تحت `sessions/_wrdiat_org/` — لا يحتاج أي تعديل في الكود.

> إن جاءت معك بيانات التطبيق السابق (عقود لا تبدأ بـ `sessions/_wrdiat_org`)، يمكنك حذفها من الجدول مباشرة بعد الاستيراد.

---

### 🔎 كيف أستخرج «فرعي» فقط من المشروع السابق؟

بياناتك موجودة داخل فرع (عقدة) اسمها `sessions` ← `_wrdiat_org` وفيها: `departments` (الأقسام) · `staff` (الموظفون) · `scheduleRules` (القواعد) · `shifts` (الورديات) · `admins` (الأدمن) · `swapLog`.

**الطريقة 1 — الأسهل (تصدير كامل ثم حذف الزائد):**
1. المشروع القديم: Realtime Database ← **Data** ← قائمة **⋮** ← **Export JSON** → ينزل ملف فيه كل القاعدة.
2. المشروع الجديد: **Data** ← **⋮** ← **Import JSON** ← اختر الملف.
   - ميزتها: تحفظ **نفس المسارات** تماماً — `sessions/_wrdiat_org/...` — بدون أي تعديل.
3. احذف ما لا تريده: في المشروع الجديد افتح `sessions` واحذف أي عقدة غير `_wrdiat_org`، واحذف أي عقدة عليا أخرى (زر ✕ بجانب العقدة) — بيانتك تبقى سليمة.

**الطريقة 2 — استخراج فرعك وحده عبر رابط (REST):**
1. افتح هذا الرابط في المتصفح (استبدل برابط قاعدتك القديمة):
   `https://ytcalender-bae88-default-rtdb.asia-southeast1.firebasedatabase.app/sessions/_wrdiat_org.json?print=pretty`
2. إذا ظهرت البيانات على الشاشة ← احفظ الصفحة: **Ctrl+S** ← `wrdiat-org.json`.
3. إذا ظهر `Permission denied` ← قواعد المشروع القديم مقفلة (انتهت مدة Test mode): افتح Rules في المشروع القديم وضع مؤقتاً `{ "rules": { ".read": true } }` ← Publish ← ثم افتح الرابط وسجّل الملف ← وبعدها أعد القواعد كما كانت.
4. الاستيراد: ملف الفرع لا يُستورد مباشرة في الجذر لأنه يحتاج أن يكون ملفوفاً باسم المسار:
```json
{ "sessions": { "_wrdiat_org": <محتوى الملف هنا> } }
```
   - أرسل لي الملف هنا وسأعيده **ملفوفاً وجاهزاً للاستيراد** خلال دقيقة، أو نفّذ اللف بنفسك بمحرر نصوص.

> ⚠️ **Import JSON يستبدل كامل قاعدة الهدف** — نفّذ الاستيراد قبل إدخال أي بيانات جديدة في مشروع wrdiat، أو تأكد أنك لا تمانع الاستبدال.

---

## 3) ربط التطبيق بالمشروع الجديد

افتح `config.js` في المستودع واستبدل قسم `firebaseConfig` بالقيم الجديدة:

```js
firebaseConfig: {
  apiKey: "القيمة الجديدة",
  authDomain: "wrdiat-XXXX.firebaseapp.com",
  databaseURL: "https://wrdiat-XXXX-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "wrdiat-XXXX",
  storageBucket: "wrdiat-XXXX.firebasestorage.app",
  messagingSenderId: "القيمة الجديدة",
  appId: "القيمة الجديدة"
}
```

ثم commit + push (عبر SSH). ← أو **أرسل لي الكائن الجديد هنا وأنا أتولى التحديث والرفع فوراً**.

**بعد الرفع:** حدّث صفحة التطبيق مرة واحدة (F5) وسيتصل بالمشروع الجديد.

---

## 4) التحقق أن كل شيء يعمل

1. افتح التطبيق ← صفحة الورديات ← اضغط **💾 حفظ** تحت أي قسم.
2. يجب أن تظهر الومضة الخضراء ✅ (بدل رسالة «تعذر الحفظ»).
3. في كونسول المشروع الجديد: Realtime Database ← Data ← ستجد `sessions/_wrdiat_org/scheduleRules` و`shifts` تتحدّث لحظياً.

---

## ملاحظة تقنية: النطاق العام في القواعد يُخزَّن باسم `_all`
فايربيس لا يسمح بمفتاح فارغ (`""`) ولا بالرموز `. # $ / [ ]` في المفاتيح، بينما التطبيق يستخدم مفتاحاً فارغاً داخلياً للنطاق العام «كل الأقسام».
لذلك: **عند الكتابة** يُحوَّل `''` → `_all` (وتُنظَّف أي رموز غير مسموحة)، و**عند القراءة** يُعاد `_all` → `''` — فلا يتأثر أي شيء في واجهة التطبيق.
> أي كتابة بمفتاح فارغ تُرفض من فايربيس بخطأ: `value argument contains an invalid key` — وهذا ما كان يمنع رفع القواعد والورديات سابقاً.

---

## 📱 شاشة البداية: أدمن أو موظف (وكيف يتعرّف الجهاز على مؤسسته)

**التطبيق لا يفتح لأي زائر:** عند الفتح تظهر **شاشة البداية** في وسط الشاشة (اسم النظام + حقل الإيميل + كلمة المرور + زر «دخول») وتحتها **«➕ تسجيل جديد (أدمن مؤسسة)»**.

| من يدخل؟ | ماذا يكتب؟ | ماذا يرى؟ |
|---|---|---|
| **الأدمن** | **إيميله** + كلمة مروره | كل شيء: الأقسام · الموظفون · الورديات · الإعدادات |
| **الموظف** | **إيميل الأدمن** + **كلمة مرور الموظفين** | ورديات **مؤسسته فقط** — عرض بدون أي تعديل |

- الأدمن يضبط كلمة مرور الموظفين من: **الإعدادات ← 🔐 الأمان** (زر «💾 حفظ الكلمة» / «إزالة»). تغييرها يُخرج الموظفين المسجّلين ويطلب منهم الدخول بالكلمة الجديدة.
- الجلسة تُحفظ على الجهاز (لا يسأل الموظف كل مرة) وتعمل بلا إنترنت، ويُخزَّن معها **مؤسسته**.

**كيف يُحدَّد مسار المؤسسة؟** لكل أدمن يسجّل **مؤسسة مستقلة**:

```
orgs/<مفتاح إيميل الأدمن>            → { email, name, passwordHash, role, orgId }
sessions/_<orgId>_org/…              → departments, staff, shifts, scheduleRules, settings
```
- المؤسسة **الأصلية** (`admin@admin.com`) بقيت على مسارها القديم `sessions/_wrdiat_org/` ⇒ **لا فقدان لأي بيانات سابقة**.
- أي أدمن جديد يُسجّل يأخذ orgId = مفتاح إيميله ⇒ بيانات كل مؤسسة **معزولة تماماً** عن غيرها، ولو كانوا على نفس المشروع.
- التخزين المحلي على الجهاز مُفصول لكل مؤسسة أيضاً (مساحة مستقلة لكل orgId).

**إضافة مؤسسة جديدة:** التسجيل مفتوح دائماً لكنه **مقفول برمز تفعيل** — راجع قسم «📋 طلبات التسجيل» بالأسفل.

**القواعد الموصى بها الآن** (توضع كما هي في Rules):

```json
{
  "rules": {
    "sessions": { "$org": { ".read": true, ".write": true } },
    "orgs": {
      ".read": true,
      "$key": {
        ".write": "!data.exists() && root.child('signups/' + $key + '/status').val() === 'pending' && newData.child('orgId').val() === $key && newData.child('role').val() === 'admin'",
        "passwordHash": { ".write": true }
      }
    },
    "signups": { ".read": true, "$key": { ".write": true } }
  }
}
```
> - `"orgs"`: لا يُنشأ سجل مؤسسة إلا لطلب تسجيل **معلّق فعلاً** (`signups/<key>` بحالة pending)، وبشرط `orgId === $key` (يمنع إنشاء حساب يوجَّه لمسار مؤسسة غيره) و`role === 'admin'`.
> - `"passwordHash": { ".write": true }` (داخل `$key`): يسمح بتغيير كلمة مرور الأدمن **فقط** — من التطبيق: الإعدادات ← 🔐 الأمان ← «تغيير كلمة المرور» (بعد التحقق من الكلمة الحالية)، أو تصفيرها من مالك النظام (📋 الطلبات ← 🔑 تصفير). بقية حقول السجل (الإيميل/الاسم/orgId) تبقى محصّنة لا تُعدَّل.
> - `"signups"`: طلبات التسجيل — الكتابة مفتوحة (صاحب الطلب يرسل بياناته، المالك يضبط `codeHash`، التفعيل يقلب الحالة إلى active).
> - بعد نشر هذه القواعد يجب أن يبقى `allowSignup: true` في `config.js` (الافتراضي) — البوابة الحقيقية هي الرمز.

⚠️ **تنبيه أمني صريح:** القراءة مفتوحة على هذا المشروع، فمن يعرف رابط قاعدة البيانات يستطيع قراءة سجلّات `orgs` (وفيها الهاش) وبيانات المؤسسات، ويستطيع الكتابة في `sessions/...`. لذلك: استخدم **كلمة مرور طويلة (12+ حرفاً)**، ولا تشارك رابط القاعدة، وللأمان الكامل لاحقاً نشغّل **Firebase Authentication** ونقفل الكتابة بـ `"auth != null"`.

---

## 🔐 منع اللعب في التسجيل: بوابة رمز التفعيل

**الفكرة:** أي زائر يستطيع إرسال **طلب** تسجيل (اسم + إيميل + جوال + كلمة مرور)، لكن **لا يُنشأ حساب ولا مؤسسة** حتى يدخل **رمز تفعيل يصدره مالك النظام** ويوصلها لصاحب الطلب بنفسه (واتساب/اتصال على الرقم الذي سجّله).

**المسار كاملاً:**
1. صاحب المؤسسة الجديدة: شاشة البداية ← «➕ تسجيل جديد» ← الاسم/الإيميل/**رقم الجوال**/كلمة المرور ← «إرسال الطلب» ← تظهر له رسالة «⏳ بانتظار رمز التفعيل» + خانة لإدخال الرمز.
2. الطلب يُحفظ في القاعدة: `signups/<مفتاح الإيميل> = { email, name, phone, passwordHash, status: 'pending', createdAt }` — **بلا أي حقل للرمز بعد**.
3. **المالك (أنت — أدمن المؤسسة الأصلية فقط):** الإعدادات ← **📋 الطلبات** ← يرى كل طلب معلّق (الاسم/الإيميل/**📱 الجوال** مع روابط اتصال وواتساب ونسخ الرقم) ← يكتب رمزاً من عنده (أو يضغط 🎲 لتوليد 4 أرقام) ← «💾 إصدار الرمز».
   - الرمز يُحفظ **مشفراً** (`codeHash` = SHA‑256 للنص `wrdiat:code:<الرمز>`) ⇒ لا يستطيع صاحب الطلب معرفته من القاعدة إطلاقاً — المصدر الوحيد له هو المالك.
   - إصدار رمز جديد لنفس الطلب يلغي القديم فوراً. وزر «🗑 حذف الطلب» يرفضه نهائياً.
4. المالك يرسل الرمز لصاحب الطلب على جواله.
5. صاحب الطلب يدخل الرمز في الخانة (أو يحاول «دخول» بإيميله فتظهر له خانة الرمز تلقائياً) ← «✅ تفعيل الحساب والدخول» ← **يُنشأ `orgs/<مفتاحه>` وتُفتح له مؤسسته مباشرة** ويصبح أدمنها، والطلب يُعلَّم `active`.

**من يرى تبويب الطلبات؟** أدمن **المؤسسة الأصلية** فقط (`orgId === 'wrdiat'`) — أدمن المؤسسات الأخرى لا يرونه ولا يستطيعون فتحه (يرجعهم التطبيق لتبويب الأقسام).

### 🔑 إدارة كلمات المرور (تبويب 🔐 الأمان + شاشة الدخول)

- **كلمة مرور الموظفين تظهر للأدمن:** تُحفظ ظاهرة في `settings/staffPassword` (بجانب الهاش) وتُعرض في تبويب الأمان داخل صندوق «🔑 كلمة المرور الحالية (حتى لا تنساها)» مع زر نسخ. الإزالة تمسح الاثنين.
- **تغيير كلمة مرور الأدمن:** الإعدادات ← 🔐 الأمان ← «👤 كلمة مرور الأدمن» — تُكتب الحالية والجديدة وتأكيدها، والتحقق من الحالية يتم قبل كتابة الهاش الجديد في `orgs/<مفتاحك>/passwordHash`. ممنوع أن تساوي كلمة الموظفين (حماية عكسية). يتطلب نشر القاعدة `passwordHash` أعلاه.
- **نسيت كلمة المرور؟** (شاشة الدخول): التطبيق لا يرسل بريداً بنفسه (موقع ثابت بلا خادم) — المسار العملي:
  1. الأدمن يضغط «❓ نسيت كلمة المرور؟» ويكتب إيميله → يظهر له زر «📧 إرسال طلب بالبريد» يفتح بريده برسالة جاهزة إلى **بريد مالك النظام** (اضبطه في `config.js` ← `supportEmail`).
  2. المالك: الإعدادات ← 📋 الطلبات ← «🏢 المؤسسات المسجّلة» ← «🔑 تصفير كلمة المرور وإرسالها له بالبريد» → تُولَّد كلمة مؤقتة (8 خانات) وتُكتب في `orgs/<مفتاحه>/passwordHash` وتظهر للمالك مع زر 📧 يفتح بريده بالكلمة الجديدة جاهزة للإرسال.
  3. صاحب المؤسسة يدخل بإيميله + الكلمة المؤقتة، ويُفضَّل أن يغيّرها فوراً من تبويب الأمان.
  > الكلمة القديمة تتوقف فور التصفير — لكن جلسة جهازه المفتوحة تبقى (بالتصميم) حتى يخرج بنفسه.
- **زر حذف جميع الورديات:** الإعدادات ← 🗓️ الورديات ← أسفل الصفحة — يمسح عقدة `shifts` في مسار المؤسسة (تأكيد مزدوج). الأقسام والموظفون والقواعد الأسبوعية تبقى، ويمكن إعادة توليد الورديات من القواعد.
- **منع تكرار الموظف في اليوم:** لو أُضيف نفس الموظف من مكانين (قاعدة عامة + قاعدة قسم، أو يدوي + تلقائي) يظهر **مرة واحدة فقط** — تُعتمد الوردية اليدوية أولاً ثم الأبكر وقتاً (في صفحة اليوم والتقويم والإحصائيات).
- **واجهة «قاعدة Firebase» والإعداد المتقدم حُذفت من الإعدادات** (بطلب المالك) — ربط القاعدة يتم من ملف `config.js` مباشرة.

**قفل التسجيل نهائياً (اختياري):** `allowSignup: false` في `config.js` يخفي زر «تسجيل جديد» ويرفض أي طلب — مثلاً في نسخة مؤسسة واحدة لا تريد ضيوفاً أصلاً. الافتراضي الآن `true` (البوابة = الرمز).

**الحماية على مستوى القواعد:** (القواعد الموصى بها أعلاه) لا يُنشأ سجل `orgs` إلا لطلب معلّق فعلاً، ولا يمكن توجيهه لمسار مؤسسة أخرى (`orgId === $key`). ملاحظة صريحة: بدون Firebase Authentication لا تستطيع القواعد التمييز بين متصفح المالك ومتصفح صاحب الطلب، فبوابة الرمز تُفسد اللعب عبر واجهة التطبيق (وهي طريقة الاستخدام الوحيدة عملياً)، ومن يعرف رابط القاعدة نفسه يستطيع تجاوزها — لهذا لا تشارك رابط القاعدة أبداً.

**للمستقبل (الأقوى):** تشغيل **Firebase Authentication** (Email/Password) وتسجيل دخول الأدمن بحساب حقيقي، ثم قواعد مثل `".write": "auth != null"` — ساعتها لا يستطيع أحد الكتابة في القاعدة أصلاً بدون حساب مصرّح.

---

## 🏢 نشر التطبيق لعدة مؤسسات (كل مؤسسة بأجهزتها)

**الخيار الأول — نسخة ورابط ومشروع لكل مؤسسة (عزل كامل، وهو الأسهل عملياً):**
1. انسخ المستودع/المجلد لكل مؤسسة (Fork أو نسخة جديدة) ← يصبح لكل مؤسسة **رابط Pages خاص**.
2. لكل مؤسسة **مشروع Firebase خاص** (قاعدة مستقلة تماماً) + نفس القواعد المنشورة.
3. عدّل `config.js` في نسخة كل مؤسسة: `org.id` + `org.name` + `org.subtitle` + `firebaseConfig` الخاص بمشروعها.
4. انسخ باقي الملفات كما هي، وارفع — وزّع رابط المؤسسة على موظفيها.
> النتيجة: جهاز الموظف يفتح رابط مؤسسته ← يتصل بقاعدة مؤسسته فقط ← يرى اسم مؤسسته في الأعلى (من `config.js`)، ويُحفظ التطبيق على جهازه (PWA) ويعمل حتى بلا إنترنت على بيانات مؤسسته.

**الخيار الثاني — موقع واحد لكل المؤسسات بمسار مختلف (`?org=`):**
- نفس الموقع، والمؤسسة تُحدَّد من الرابط (`?org=alaa`) أو من اختيار محفوظ على الجهاز، والبيانات تُخزّن تحت `sessions/_<org>_org/`.
- يحتاج تعديلاً بسيطاً: استخدام `ORG` في `path()` (بدل المسار الثابت) + قراءة `?org=` + حفظ اختيار الجهاز + لافتة باسم المؤسسة.
- ⚠️ تنبيه: الطرفان يشتركان في نفس قاعدة البيانات ⇒ العزل أقل (الأفضل مشروع Firebase لكل مؤسسة أو Firebase Auth).

---

## 5) حذف البيانات من المشروع السابق

> ⚠️ قبل أي حذف: اعمل **نسخة احتياطية** (Realtime Database ← Data ← ⋮ ← **Export JSON**) — الحذف نهائي ولا رجعة فيه.

**حذف فرع البيانات فقط (مستحسن):**
1. افتح المشروع القديم ← **Realtime Database** ← تبويب **Data**.
2. وسّع العقدة `sessions`.
3. مرّر المؤشر على العقدة `_wrdiat_org` ← اضغط **✕** (تظهر عند المرور) ← أكّد الحذف.
   - أو احذف العقدة `sessions` كاملة إن لم يكن فيها شيء تحتاجه.
4. تأكد من التطبيق: افتح قاعدتك الجديدة ← Data ← البيانات موجودة تحت `sessions/_wrdiat_org/` ✅

> ⚠️ **تنبيه مهم:** إذا كان **التطبيق الثاني** يقرأ من نفس المسار `sessions/_wrdiat_org` فحذفه سيمسح بياناته هو أيضاً — تأكد قبل الحذف. إن كان يستخدم مساراً/مشروعاً آخر فلا مشكلة.

**حذف المشروع القديم بالكامل (لو لم يعد مستخدماً):**
1. ⚙️ **Project settings** ← **General** ← انزل لآخر الصفحة ← **Delete project**.
2. اكتب معرّف المشروع للتأكيد ← Delete.
> ⚠️ يحذف المشروع نهائياً بكل تطبيقاته وبياناته — لا تنفّذه إن كان أي تطبيق آخر يستخدمه. ولا يؤثر على مستودع GitHub ولا على ملفات التطبيق.

---

## ملاحظات مهمة

- **التشارك مع التطبيق الثاني سينقطع:** بعد الانتقال يعيش wrdiat في مشروعه الخاص، وأي تعديل فيه لا يصل للتطبيق الثاني (والعكس). إن أردت الإبقاء على التشارك، الأفضل عدم الانتقال وإصلاح قواعد المشروع القديم بدلاً من ذلك.
- **المشروع القديم يبقى كما هو** وبياناته محفوظة فيه — يمكنك الرجوع إليه أي وقت بإعادة القيم القديمة في `config.js`.
- **مشكلة «تعذر الحفظ على الخادم» سببها الأرجح** أن قواعد المشروع القديم (Test mode) انتهت مدة صلاحيتها — لذلك الكتابة مرفوضة. الانتقال للمشروع الجديد بالقواعد المذكورة أعلاه يحل المشكلة نهائياً.
- البيانات المحفوظة على الأجهزة (localStorage) لا تُمس عند الانتقال — ولو كان فيها تعديلات غير مُزامنة فستُرفع للمشروع الجديد تلقائياً.
