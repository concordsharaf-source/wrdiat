# الانتقال إلى مشروع Firebase جديد (wrdiat) — خطوة بخطوة

التطبيق الآن مربوط بالمشروع القديم `ytcalender-bae88` (من التطبيق السابق)، وكل البيانات فيه تحت
`sessions/_wrdiat_org/`. للانتقال إلى مشروعك الجديد **wrdiat** اتبع الخطوات بالترتيب.

---

## 1) تهيئة المشروع الجديد في الكونسول

**أ. إنشاء قاعدة بيانات Realtime Database (مهم — المشروع الجديد لا يملك قاعدة تلقائياً):**
1. افتح <https://console.firebase.google.com> ← اختر مشروع **wrdiat**.
2. القائمة الجانبية: **Build ← Realtime Database** ← **Create Database**.
3. اختر الموقع الأقرب (الأفضل نفس القديم: **asia-southeast1 / Singapore**).
4. اختر **Start in locked mode** (مقفلة) — لا تختر Test mode لأن صلاحيتها تنتهي بعد 30 يوماً وتتوقف الكتابة.
5. بعد الإنشاء: تبويب **Rules** ← الصق ثم **Publish**:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

> **بخصوص رسالة فايربيس:** «Your security rules are defined as public…» — هذه **تنبيه طبيعي** يظهر لأي قواعد عامة، ومعناه أن أي شخص يعرف رابط القاعدة يستطيع القراءة والكتابة مباشرة. وليست خطأ يمنع العمل.

### خيارات الأمان (اختر واحداً)

**الخيار 1 — عامة كما هي (الأبسط):** مناسبة لتطبيق داخلي صغير. التطبيق يحمي الواجهة بتسجيل دخول الأدمن، لكن من يعرف رابط القاعدة يستطيع الكتابة مباشرة.

**الخيار 2 — قواعد أضيق على مسار التطبيق فقط (بدون أي تعديل كود):** يقلّل المخاطرة لبقية محتوى القاعدة:

```json
{
  "rules": {
    "sessions": {
      "_wrdiat_org": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```

**الخيار 3 — تشديد كامل بتسجيل دخول Firebase (الأقوى، يحتاج تعديل كود في التطبيق):**
يُسجَّل الأدمن في Firebase Auth عند دخوله، وتصبح الكتابة محصورة بالمُسجَّلين:
`.read: true` للجميع (الاطلاع) + `.write: "auth != null"`.
بها تختفي إمكانية التعديل من خارج التطبيق — أخبرني لو أردتها وأضيفها.

---

### ⚠️ خطوة حرجة: هل أنشأت قاعدة البيانات بالوضع الصحيح؟

**لا تختر «Start in test mode»** عند الإنشاء — صلاحيته تنتهي بعد 30 يوماً وتتوقف الكتابة (وهذا هو سبب رسالة «تعذر الحفظ على الخادم» في المشروع القديم).
اختر **Start in locked mode** ثم الصق القواعد أعلاه في تبويب **Rules** واضغط **Publish**.
6. انسخ رابط القاعدة الظاهر أعلى تبويب **Data** — سيكون بهذا الشكل:
   `https://wrdiat-XXXXX-default-rtdb.asia-southeast1.firebasedatabase.app`

**ب. تسجيل تطبيق ويب لِجلب الإعدادات:**
1. ⚙️ **Project settings** ← تبويب **General** ← انزل إلى **Your apps**.
2. اضغط أيقونة **`</>`** (Web) ← اكتب الاسم `wrdiat-web` ← **Register app** (لا تحتاج Hosting).
3. سيظهر كائن **firebaseConfig** — انسخ القيم كلها (apiKey / authDomain / **databaseURL** / projectId / storageBucket / messagingSenderId / appId).

---

## 2) نقل البيانات القديمة (إن أردت نفس الأقسام والموظفين والورديات)

1. **من المشروع القديم** `ytcalender-bae88`: Realtime Database ← تبويب **Data**.
2. من قائمة **⋮** (أعلى يمين الجدول) اختر **Export JSON** — ينزل ملف على جهازك.
3. **في المشروع الجديد wrdiat**: Realtime Database ← **Data** ← **⋮** ← **Import JSON**.
4. اختر الملف ← Import (⚠️ الاستيراد يستبدل كامل قاعدة البيانات الجديدة).
5. ستجد نفس البنية تحت `sessions/_wrdiat_org/` — لا يحتاج أي تعديل في الكود.

> إن جاءت معك بيانات التطبيق السابق (عقود لا تبدأ بـ `sessions/_wrdiat_org`)، يمكنك حذفها من الجدول مباشرة بعد الاستيراد.

---

### 🔎 كيف أستخرج «فرعي» فقط من المشروع السابق؟

بياناتك موجودة داخل فرع (عقدة) اسمها `sessions` ← `_wrdiat_org` وفيها: `departments` (الأقسام) · `staff` (الموظفون) · `scheduleRules` (القواعد) · `shifts` (الورديات) · `admins` (الأدمن) · `swapLog`.

**الطريقة 1 — الأسهل (تصدير كامل ثم حذف الزائد):**
1. المشروع القديم: Realtime Database ← **Data** ← قائمة **⋮** ← **Export JSON** → ينزل ملف فيه كل القاعدة.
2. المشروع الجديد: **Data** ← **⋮** ← **Import JSON** ← اختر الملف.
   - ميزتها: تحفظ **نفس المسارات** تماماً — `sessions/_wrdiat_org/...` — بدون أي تعديل.
3. احذف ما لا تريده: في المشروع الجديد افتح `sessions` واحذف أي عقدة غير `_wrdiat_org`، واحذف أي عقدة عليا أخرى (زر ✕ بجانب العقدة) — بيانتك تبقى سليمة.

**الطريقة 2 — استخراج فرعك وحده عبر رابط (REST):**
1. افتح هذا الرابط في المتصفح (استبدل برابط قاعدتك القديمة):
   `https://ytcalender-bae88-default-rtdb.asia-southeast1.firebasedatabase.app/sessions/_wrdiat_org.json?print=pretty`
2. إذا ظهرت البيانات على الشاشة ← احفظ الصفحة: **Ctrl+S** ← `wrdiat-org.json`.
3. إذا ظهر `Permission denied` ← قواعد المشروع القديم مقفلة (انتهت مدة Test mode): افتح Rules في المشروع القديم وضع مؤقتاً `{ "rules": { ".read": true } }` ← Publish ← ثم افتح الرابط وسجّل الملف ← وبعدها أعد القواعد كما كانت.
4. الاستيراد: ملف الفرع لا يُستورد مباشرة في الجذر لأنه يحتاج أن يكون ملفوفاً باسم المسار:
```json
{ "sessions": { "_wrdiat_org": <محتوى الملف هنا> } }
```
   - أرسل لي الملف هنا وسأعيده **ملفوفاً وجاهزاً للاستيراد** خلال دقيقة، أو نفّذ اللف بنفسك بمحرر نصوص.

> ⚠️ **Import JSON يستبدل كامل قاعدة الهدف** — نفّذ الاستيراد قبل إدخال أي بيانات جديدة في مشروع wrdiat، أو تأكد أنك لا تمانع الاستبدال.

---

## 3) ربط التطبيق بالمشروع الجديد

افتح `config.js` في المستودع واستبدل قسم `firebaseConfig` بالقيم الجديدة:

```js
firebaseConfig: {
  apiKey: "القيمة الجديدة",
  authDomain: "wrdiat-XXXX.firebaseapp.com",
  databaseURL: "https://wrdiat-XXXX-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "wrdiat-XXXX",
  storageBucket: "wrdiat-XXXX.firebasestorage.app",
  messagingSenderId: "القيمة الجديدة",
  appId: "القيمة الجديدة"
}
```

ثم commit + push (عبر SSH). ← أو **أرسل لي الكائن الجديد هنا وأنا أتولى التحديث والرفع فوراً**.

**بعد الرفع:** حدّث صفحة التطبيق مرة واحدة (F5) وسيتصل بالمشروع الجديد.

---

## 4) التحقق أن كل شيء يعمل

1. افتح التطبيق ← صفحة الورديات ← اضغط **💾 حفظ** تحت أي قسم.
2. يجب أن تظهر الومضة الخضراء ✅ (بدل رسالة «تعذر الحفظ»).
3. في كونسول المشروع الجديد: Realtime Database ← Data ← ستجد `sessions/_wrdiat_org/scheduleRules` و`shifts` تتحدّث لحظياً.

---

## ملاحظة تقنية: النطاق العام في القواعد يُخزَّن باسم `_all`
فايربيس لا يسمح بمفتاح فارغ (`""`) ولا بالرموز `. # $ / [ ]` في المفاتيح، بينما التطبيق يستخدم مفتاحاً فارغاً داخلياً للنطاق العام «كل الأقسام».
لذلك: **عند الكتابة** يُحوَّل `''` → `_all` (وتُنظَّف أي رموز غير مسموحة)، و**عند القراءة** يُعاد `_all` → `''` — فلا يتأثر أي شيء في واجهة التطبيق.
> أي كتابة بمفتاح فارغ تُرفض من فايربيس بخطأ: `value argument contains an invalid key` — وهذا ما كان يمنع رفع القواعد والورديات سابقاً.

---

## 5) حذف البيانات من المشروع السابق

> ⚠️ قبل أي حذف: اعمل **نسخة احتياطية** (Realtime Database ← Data ← ⋮ ← **Export JSON**) — الحذف نهائي ولا رجعة فيه.

**حذف فرع البيانات فقط (مستحسن):**
1. افتح المشروع القديم ← **Realtime Database** ← تبويب **Data**.
2. وسّع العقدة `sessions`.
3. مرّر المؤشر على العقدة `_wrdiat_org` ← اضغط **✕** (تظهر عند المرور) ← أكّد الحذف.
   - أو احذف العقدة `sessions` كاملة إن لم يكن فيها شيء تحتاجه.
4. تأكد من التطبيق: افتح قاعدتك الجديدة ← Data ← البيانات موجودة تحت `sessions/_wrdiat_org/` ✅

> ⚠️ **تنبيه مهم:** إذا كان **التطبيق الثاني** يقرأ من نفس المسار `sessions/_wrdiat_org` فحذفه سيمسح بياناته هو أيضاً — تأكد قبل الحذف. إن كان يستخدم مساراً/مشروعاً آخر فلا مشكلة.

**حذف المشروع القديم بالكامل (لو لم يعد مستخدماً):**
1. ⚙️ **Project settings** ← **General** ← انزل لآخر الصفحة ← **Delete project**.
2. اكتب معرّف المشروع للتأكيد ← Delete.
> ⚠️ يحذف المشروع نهائياً بكل تطبيقاته وبياناته — لا تنفّذه إن كان أي تطبيق آخر يستخدمه. ولا يؤثر على مستودع GitHub ولا على ملفات التطبيق.

---

## ملاحظات مهمة

- **التشارك مع التطبيق الثاني سينقطع:** بعد الانتقال يعيش wrdiat في مشروعه الخاص، وأي تعديل فيه لا يصل للتطبيق الثاني (والعكس). إن أردت الإبقاء على التشارك، الأفضل عدم الانتقال وإصلاح قواعد المشروع القديم بدلاً من ذلك.
- **المشروع القديم يبقى كما هو** وبياناته محفوظة فيه — يمكنك الرجوع إليه أي وقت بإعادة القيم القديمة في `config.js`.
- **مشكلة «تعذر الحفظ على الخادم» سببها الأرجح** أن قواعد المشروع القديم (Test mode) انتهت مدة صلاحيتها — لذلك الكتابة مرفوضة. الانتقال للمشروع الجديد بالقواعد المذكورة أعلاه يحل المشكلة نهائياً.
- البيانات المحفوظة على الأجهزة (localStorage) لا تُمس عند الانتقال — ولو كان فيها تعديلات غير مُزامنة فستُرفع للمشروع الجديد تلقائياً.
